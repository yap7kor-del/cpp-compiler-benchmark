name: C/C++ CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  # Job 1: Static Analysis (Run before compiling to catch errors early)
  static-analysis:
    name: Static Analysis & Linting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python (For Conan)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Conan & Static Analysis Tools
        run: |
          pip install conan
          conan profile detect --force
          sudo apt-get update
          sudo apt-get install -y cppcheck clang-tidy

      - name: Install Dependencies with Conan
        run: |
          conan install . --build=missing -s build_type=Release

      - name: Generate Compile Commands with CMake
        run: |
          cmake -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE=build/Release/generators/conan_toolchain.cmake \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Run Cppcheck
        run: |
          cppcheck --enable=all --inconclusive --std=c++20 --suppress=missingIncludeSystem src/

      - name: Run Clang-Tidy
        run: |
          clang-tidy -p build src/math/*.cpp src/dsp/*.cpp src/main.cpp

  # Job 2: Build Matrix for Compilers, Architectures, and Optimizations
  build-and-test:
    name: Build (${{ matrix.compiler }}, ${{ matrix.arch }}, ${{ matrix.opt_flag }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # --- GCC builds on Linux (x86_64) ---
          - os: ubuntu-latest
            compiler: gcc
            cxx: g++
            arch: x86_64
            opt_flag: -O0
            build_type: Debug
          - os: ubuntu-latest
            compiler: gcc
            cxx: g++
            arch: x86_64
            opt_flag: -O3
            build_type: Release

          # --- Clang builds on Linux (x86_64) ---
          - os: ubuntu-latest
            compiler: clang
            cxx: clang++
            arch: x86_64
            opt_flag: -O2
            build_type: Release
          - os: ubuntu-latest
            compiler: clang
            cxx: clang++
            arch: x86_64
            opt_flag: -Ofast
            build_type: Release

          # --- Cross-Compilation for ARM (Cortex-M4) with QEMU ---
          - os: ubuntu-latest
            compiler: arm-none-eabi-gcc
            cxx: arm-none-eabi-g++
            arch: arm-cortex-m4
            # Keep compiler flags clean (no linker specs here)
            opt_flag: -Os -mcpu=cortex-m4 -mthumb
            build_type: MinSizeRel

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python (For Conan)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Conan
        run: |
          pip install conan

      - name: Install System Compilers & Emulators
        run: |
          sudo apt-get update
          if [ "${{ matrix.compiler }}" = "gcc" ]; then
            sudo apt-get install -y g++
          elif [ "${{ matrix.compiler }}" = "clang" ]; then
            sudo apt-get install -y clang
            sudo update-alternatives --install /usr/bin/cc cc /usr/bin/clang 100
            sudo update-alternatives --install /usr/bin/c++ c++ /usr/bin/clang++ 100
            sudo update-alternatives --set cc /usr/bin/clang
            sudo update-alternatives --set c++ /usr/bin/clang++
          elif [ "${{ matrix.compiler }}" = "arm-none-eabi-gcc" ]; then
            # Install arm-none-eabi toolchain, newlib libraries, and QEMU ARM system emulator
            sudo apt-get install -y gcc-arm-none-eabi libnewlib-arm-none-eabi libstdc++-arm-none-eabi-newlib qemu-system-arm
          fi
          if [ "${{ matrix.compiler }}" != "arm-none-eabi-gcc" ]; then
            echo "CC=${{ matrix.compiler }}" >> $GITHUB_ENV
            echo "CXX=${{ matrix.cxx }}" >> $GITHUB_ENV
          fi

      - name: Configure Conan Profile
        run: |
          conan profile detect --force

      - name: Run Conan Install
        run: |
          if [ "${{ matrix.compiler }}" = "arm-none-eabi-gcc" ]; then
            conan install --requires="fmt/11.0.2" -g CMakeDeps -s build_type=${{ matrix.build_type }} -o "fmt/*:header_only=True" --output-folder="build/${{ matrix.build_type }}/generators"
          else
            conan install . --build=missing -s build_type=${{ matrix.build_type }}
          fi

      - name: Configure CMake
        run: |
          TOOLCHAIN_ARG=""
          if [ -f "build/${{ matrix.build_type }}/generators/conan_toolchain.cmake" ]; then
            TOOLCHAIN_ARG="-DCMAKE_TOOLCHAIN_FILE=build/${{ matrix.build_type }}/generators/conan_toolchain.cmake"
          else
            TOOLCHAIN_ARG="-DCMAKE_PREFIX_PATH=build/${{ matrix.build_type }}/generators"
          fi
          cmake -B build \
            $TOOLCHAIN_ARG \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DCMAKE_CXX_FLAGS="${{ matrix.opt_flag }}" \
            -DCMAKE_CXX_COMPILER=${{ matrix.cxx }}

      - name: Build Project
        run: |
          cmake --build build --parallel

      # Native benchmark for x86_64 builds
      - name: Run Benchmarks (Native OS)
        if: matrix.compiler != 'arm-none-eabi-gcc'
        run: |
          ./build/EmbeddedApp

      # Emulated execution for ARM Cortex-M4
      - name: Run Embedded Binary (via QEMU Emulation)
        if: matrix.compiler == 'arm-none-eabi-gcc'
        run: |
          # Print binary memory section sizes (FLASH / RAM consumption)
          arm-none-eabi-size build/EmbeddedApp

          # Run under QEMU ARM Cortex-M emulation with semihosting enabled
          # Using timeout to ensure pipeline terminates if execution loops indefinitely
          timeout 30s qemu-system-arm \
            -M lm3s6965evb \
            -cpu cortex-m4 \
            -nographic \
            -semihosting-config enable=on,target=native \
            -kernel build/EmbeddedApp || exit_code=$?
          
          # Allow clean exit (0) or timeout/halt status codes as successful completion
          if [ ${exit_code:-0} -eq 0 ] || [ ${exit_code:-0} -eq 124 ]; then
            echo "QEMU execution finished successfully."
          else
            echo "QEMU failed with exit code $exit_code"
            exit $exit_code
          fi
